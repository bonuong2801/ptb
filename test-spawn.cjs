const { spawn } = require("child_process"); const bin = require("cloudflared").bin; const child = spawn(bin, ["tunnel", "--url", "http://localhost:3001"]); child.stderr.on("data", data => { const str = data.toString(); const match = str.match(/https:\/\/[a-z0-9-]+\.trycloudflare\.com/); if (match) { console.log("FOUND URL:", match[0]); child.kill(); process.exit(0); } }); child.stdout.on("data", data => console.log(data.toString()));
